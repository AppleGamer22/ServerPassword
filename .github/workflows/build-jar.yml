#name: build-jar
#on:
#  release:
#    types: [published]
#jobs:
#  build-jar:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2.3.2
#      - name: Set up JDK 1.8
#        uses: actions/setup-java@v1
#        with:
#          java-version: 1.8
#      - name: Grant execute permission for gradlew
#        run: chmod +x gradlew
#      - name: Build with Gradle
#        run: ./gradlew shadowJar
#      - name: Upload Release Asset
#        uses: actions/upload-release-asset@v1
#        id: upload-release-asset
#        with:
#          upload_url: https://github.com/${GITHUB_REPOSITORY#*/}/sp/releases/download/${GITHUB_REF##*/}/sp-${GITHUB_REF##*/}-all.jar
#          asset_path: .${GITHUB_WORKSPACE}/build/libs/sp-${GITHUB_REF##*/}-all.jar
#          asset_name: sp-${GITHUB_REF##*/}-all.jar
#          asset_content_type: application/java-archive
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#          GITHUB_REPOSITORY: ${{ secrets.GITHUB_REPOSITORY }}
#          GITHUB_WORKSPACE: ${{ secrets.GITHUB_WORKSPACE }}